<UserControl x:Class="CarDealer.UI.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarDealer.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBox x:Name="userName" HorizontalAlignment="Left" Height="23" Margin="272,136,0,0" TextWrapping="Wrap" Text="{Binding UserName, Mode=TwoWay}" VerticalAlignment="Top" Width="120"/>
        <PasswordBox x:Name="password" HorizontalAlignment="Left" Height="23" Margin="272,182,0,0"  VerticalAlignment="Top" Width="120"/>
        <Button Content="Log In" CommandParameter="{Binding ElementName=password}" Command="{Binding LoginUserCommand}" HorizontalAlignment="Left" Margin="300,237,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</UserControl>
